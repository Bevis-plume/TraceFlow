# =============================================================================
# TraceFlow — Python Dependencies
# =============================================================================
# Install with:  pip install -r requirements.txt
# Tested on Python 3.10 / 3.11, CUDA 11.8 / 12.1

# --- Core deep learning ---
torch>=2.2.0
torchvision>=0.17.0

# --- Configuration ---
pyyaml>=6.0.1

# --- Progress bars ---
tqdm>=4.66.0

# --- Numerical computing ---
numpy>=1.26.0

# --- Image quality metrics / paper evaluation ---
scikit-image>=0.22.0
torchmetrics>=1.4.0
torch-fidelity>=0.3.0
lpips>=0.1.4

# --- Visualisation ---
matplotlib>=3.8.0
Pillow>=10.2.0

# --- CUDA / full-run dependencies ---
# Required for diffusers AutoencoderKL-based CUDA experiments.
diffusers>=0.30.0
transformers>=4.44.0
accelerate>=0.34.0
safetensors>=0.4.4

# --- Experiment tracking (optional) ---
# Uncomment if using Weights & Biases:
# wandb>=0.16.0

# --- Type checking / linting (dev only) ---
# mypy>=1.8.0
# ruff>=0.3.0
