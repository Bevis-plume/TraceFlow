# TraceFlow method fragment: keyed latent transform without watermark.
project:
  seed: 42
  device: cuda
  output_dir: outputs/flow_transformer
security:
  latent_transform:
    type: keyed
    secret_key: CHANGE_ME_IN_LOCAL_COPY
    block_size: 16
    bias_scale: 0.1
watermark:
  enabled: false
